---
import AppLayout from '../../components/layout/AppLayout.astro';
import CatalogDetailPage from '../../components/pages/CatalogDetailPage.astro';
import { serverApiFetch } from '../../lib/api/server';
import type { AlbumDetail } from '../../lib/api/types';
const { id = '' } = Astro.params;
const transitionName = Astro.url.searchParams.get('vt') ?? undefined;
const initialDetail = await serverApiFetch<AlbumDetail>(Astro.request, `/albums/${id}`);
---

<AppLayout title="Album · Spotrak">
  <CatalogDetailPage kind="album" {id} {initialDetail} {transitionName} />
</AppLayout>
