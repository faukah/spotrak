---
import CatalogDetailPage from '../../../../components/pages/CatalogDetailPage.astro';
import PublicPageShell from '../../../../components/pages/PublicPageShell.astro';
import type { TrackDetail } from '../../../../lib/api/types';
import { publicDetailContext } from '../../../../lib/public-share';
const { token = '', id = '' } = Astro.params;
const transitionName = Astro.url.searchParams.get('vt') ?? undefined;
const { apiPrefix, valid, initialDetail } = await publicDetailContext<TrackDetail>(Astro.request, token, `/tracks/${id}`);
---

<PublicPageShell title="Shared track · Spotrak" {token} {valid}>
  <CatalogDetailPage kind="track" {id} {apiPrefix} {initialDetail} {transitionName} />
</PublicPageShell>
